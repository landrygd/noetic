import { CollectionToIconPipe } from './collection-to-icon.pipe';

describe('CollectionToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CollectionToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
